name: 🐍 Generate Snake

on:
  schedule:
    - cron: "0 3 * * *"

  workflow_dispatch:

jobs:
  generate:
    permissions:
      contents: write

    runs-on: ubuntu-latest
    timeout-minutes: 5

    steps:
      - name: Gerar cobrinha dos commits
        uses: Platane/snk/svg-only@v3
        with:
          github_user_name: PedroYokada
          outputs: |
            dist/github-contribution-grid-snake.svg?color_snake=%2336BCF7
            dist/github-contribution-grid-snake-dark.svg?palette=github-dark&color_snake=%2336BCF7

      - name: Publicar animação
        uses: crazy-max/ghaction-github-pages@v3.1.0
        with:
          target_branch: output
          build_dir: dist
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}